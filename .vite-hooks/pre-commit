#!/bin/sh

# Protect main branch — all work happens on feature branches
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
  echo "ERROR: Direct commits to main are not allowed. Use a feature branch and PR."
  exit 1
fi

vp staged
