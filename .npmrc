# Supply-chain hardening: reject packages published less than 3 days ago.
# npm's min-release-age unit is DAYS (npm 11.10+) — not seconds.
min-release-age=3

# Supply-chain hardening: disable lifecycle scripts (postinstall, etc.)
ignore-scripts=true
